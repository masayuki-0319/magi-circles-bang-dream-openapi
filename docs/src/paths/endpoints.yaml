get:
  summary: All Endpoints
  description: All Endpoints
  deprecated: false
  tags:
    - endpoints
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Endpoints'
